{
  "name": "NoTouchChallenge",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir public && cp src/site/* public/"
  },
  "devDependencies": {},
  "dependencies": {
    "atob": "^2.1.2",
    "googleapis": "^48.0.0"
  }
}
